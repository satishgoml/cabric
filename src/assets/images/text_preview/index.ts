import { IMAGES_FOLDER } from "@/assets/config"

const TEXT_PREVIEW_FOLDER = IMAGES_FOLDER + "/text_preview"

const TEXT_PREVIEW = {
  TEXT_1: TEXT_PREVIEW_FOLDER + "/1.png",
  TEXT_2: TEXT_PREVIEW_FOLDER + "/2.png",
}

export default TEXT_PREVIEW
